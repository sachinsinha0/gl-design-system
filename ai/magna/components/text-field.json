{
  "id": "text-field",
  "name": "TextField",
  "dsId": "magna",
  "summary": "Single-line text input built on Tamagui Input. Uses M3 surface + outline tokens. Always pair with a `<label>` (or `Typography` label slot) — placeholder text never substitutes for a label.",
  "anatomy": [
    { "part": "label", "description": "`Typography subtitle2` in `$onSurfaceVariant`; required.", "required": true },
    { "part": "container", "description": "Field surface — `outlined` (border `$outline`) or `filled` (`$surfaceVariant`)." },
    { "part": "leading-adornment", "description": "Optional Lucide icon or small text (units)." },
    { "part": "input", "description": "`body1` text in `$onSurface`; placeholder in `$onSurfaceVariant`." },
    { "part": "trailing-icon", "description": "Optional icon (clear, password reveal)." },
    { "part": "helper-text", "description": "`caption` in `$onSurfaceVariant` (or `$error` when invalid)." }
  ],
  "props": [
    { "name": "variant", "type": "'outlined' | 'filled'", "default": "outlined" },
    { "name": "label", "type": "string", "required": true },
    { "name": "value", "type": "string", "required": true },
    { "name": "onChangeText", "type": "(v: string) => void", "required": true },
    { "name": "placeholder", "type": "string" },
    { "name": "helperText", "type": "string" },
    { "name": "error", "type": "boolean", "default": false },
    { "name": "disabled", "type": "boolean", "default": false },
    { "name": "leading", "type": "ReactNode", "description": "Lucide icon." },
    { "name": "trailing", "type": "ReactNode" },
    { "name": "secureTextEntry", "type": "boolean", "default": false }
  ],
  "variants": [
    { "axis": "variant", "values": ["outlined", "filled"], "default": "outlined" },
    { "axis": "size", "values": ["sm", "md"], "default": "md" }
  ],
  "states": [
    { "name": "rest", "description": "Border `$outline`; label `$onSurfaceVariant`." },
    { "name": "hover", "description": "Border `$onSurface` at 8% state-layer." },
    { "name": "focus", "description": "Border `$primary` 2px; label `$primary`." },
    { "name": "filled", "description": "Value rendered in `$onSurface`." },
    { "name": "error", "description": "Border + helper text in `$error`; pair with an inline icon, never color alone." },
    { "name": "disabled", "description": "Container + label at 38% emphasis." }
  ],
  "a11y": [
    "Every TextField MUST have an associated label (visible or via accessibilityLabel).",
    "Error state pairs `$error` color with the helper message — never color alone.",
    "Validate on blur, not on every keystroke (matches M3 input conventions).",
    "Touch target ≥ 48×48dp."
  ],
  "examples": [
    {
      "label": "Outlined with helper",
      "language": "tsx",
      "snippet": "import { TextField } from '@gl/elements';\n\n<TextField\n  label=\"Email\"\n  value={email}\n  onChangeText={setEmail}\n  helperText=\"We'll never share this.\"\n/>"
    },
    {
      "label": "Error state",
      "language": "tsx",
      "snippet": "<TextField label=\"Password\" value={pw} onChangeText={setPw} error helperText=\"At least 8 characters.\" secureTextEntry />"
    }
  ],
  "doNot": [
    "Use a placeholder as a replacement for a label.",
    "Show an error border without an accompanying error message.",
    "Disable a field with `opacity: 0.5` — use the disabled variant.",
    "Set the input text in `$primary` to make it stand out."
  ],
  "tokensUsed": ["$primary", "$onSurface", "$onSurfaceVariant", "$surfaceVariant", "$outline", "$error", "$errorContainer", "$onErrorContainer"]
}
