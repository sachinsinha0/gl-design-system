nodeLinker: node-modules

enableScripts: true

# Keep Tamagui + react-native-web + Jedi MUI peer-dep variants installable
# without strict peer-dep failures during the multi-DS migration.
# (yarn 4 is stricter than npm; without these many DS combos fail to install.)
nmHoistingLimits: none
